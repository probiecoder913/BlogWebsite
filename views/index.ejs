<%-include("partials/header")-%>
    <h1>Home</h1>
    <p> <%= homeContent %> </p>
  <%#  console.log(allPosts)%> 
<ul>
 
  <% allPosts.forEach(function(post){ %>
       <h2> <%= post.title %> </h2> 
        <p id="target"><%= post.body.slice(0,100)%>... <a href= "/posts/<%=post.title%>" >Read More</a></p>
    <% }) %>
</ul>
   <%- include("partials/footer") -%> 
